Comparing or hashing an *attrs* instance before all of its attributes are initialized (for example one created via `object.__new__`) now raises `attrs.exceptions.NotInitializedError` instead of leaking an `AttributeError`.
